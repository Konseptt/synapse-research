{
  "name": "synapse-research",
  "private": true,
  "description": "Monorepo root — deploy the frontend/ app on Vercel (Root Directory = frontend).",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "test": "npm test --prefix frontend"
  },
  "engines": {
    "node": ">=20"
  }
}
