{
  "name": "synapse-research",
  "private": true,
  "description": "Synapse — biomedical research intelligence (Next.js app in frontend/)",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run vercel-build --prefix frontend",
    "test": "npm test --prefix frontend",
    "db:push": "npm run db:push --prefix frontend"
  },
  "engines": {
    "node": ">=20"
  }
}
